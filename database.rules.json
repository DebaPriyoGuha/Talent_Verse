{
  "rules": {
    ".read":  "auth != null && auth.token.email === 'debapriyoguha@gmail.com'",
    ".write": "auth != null && auth.token.email === 'debapriyoguha@gmail.com'",
    "users": {
      "$uid": {
        ".read":  "auth != null",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "posts": {
      ".read": "auth != null",
      "$postId": {
        ".write": "auth != null && (!data.exists() || data.child('uid').val() === auth.uid || auth.token.email === 'debapriyoguha@gmail.com')",
        "likes": {
          "$uid": {
            ".write": "auth != null && auth.uid === $uid"
          }
        },
        "likeCount": {
          ".write": "auth != null"
        },
        "ratings": {
          "$uid": {
            ".write": "auth != null && auth.uid === $uid"
          }
        },
        "ratingAvg": {
          ".write": "auth != null"
        },
        "ratingCount": {
          ".write": "auth != null"
        },
        "comments": {
          "$commentId": {
            ".write": "auth != null"
          }
        },
        "commentCount": {
          ".write": "auth != null"
        }
      }
    }
  }
}
